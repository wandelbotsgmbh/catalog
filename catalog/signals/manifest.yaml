# yaml-language-server: $schema=../../schema.json
---
version: 0.1.4
beta: true
# Remove the Beta label here as soon as the App Store supports `beta: true` above.
name: Signals (Beta)
supportsCloud: true
description: |
  Monitor and control robot I/O signals.
homepage: https://github.com/wandelbotsgmbh
app:
  name: signals-app
  appIcon: icon.png
  containerImage:
    image: wandelbots.azurecr.io/nova-apps/signals-app:0.1.4
    secrets:
      - name: pull-secret-wandelbots-azurecr-io
  port: 3000
